\documentclass[convert={density=300,size=1080x800,outext=.png}]{standalone}
\usepackage{tkz-graph}
\usetikzlibrary{arrows,positioning,shapes,shapes.multipart,patterns,mindmap,shadows}
\usepackage{xcolor}
\usepackage{helvet}

\renewcommand{\familydefault}{\sfdefault}

\definecolor{blue}{RGB}{4, 83, 156}  % hex: #04539C
\definecolor{gray}{RGB}{148, 147, 132}  % hex: #8A9384
\definecolor{yellow}{RGB}{251, 187, 6}  % hex: #FBBB06

\begin{document}

\begin{tiny}
    \resizebox{10cm}{!}{\begin{tikzpicture}[node distance=1cm, auto]

        \node (3) [
            rectangle split,
            rectangle split parts=6,
            rectangle split part fill={
                gray,
                blue!75,
                blue!75,
                blue!75,
                blue!75,
                blue!50,
                blue!50,
                blue!50,
            },
            draw,
            text width=2.50cm
        ]
        {
            \nodepart{one}
                \begin{footnotesize}
                    src/template\_project
                \end{footnotesize}
            \nodepart{two}
                \texttt{analysis}
            \nodepart{three}
                \texttt{data}
            \nodepart{four}
                \texttt{data\_management}
            \nodepart{five}
                \texttt{final}
            \nodepart{six}
                \texttt{config.py}
        };

        \node (5) [
            rectangle split,
            rectangle split parts=3,
            rectangle split part fill={
                gray,
                blue!50,
                blue!50,
            },
            draw,
            text width=3.0cm,
            right=of 3
        ]
        {
            \nodepart{one}
                \begin{footnotesize}
                    data\_management
                \end{footnotesize}
            \nodepart{two}
                \texttt{\_\_init\_\_.py}
            \nodepart{three}
                \texttt{clean\_data.py}
        };

        \node (4) [
            rectangle split,
            rectangle split parts=4,
            rectangle split part fill={
                gray,
                blue!50,
                blue!50,
                blue!50,
            },
            draw,
            text width=2.5cm,
            above=of 5
        ]
        {
            \nodepart{one}
                \begin{footnotesize}
                    analysis
                \end{footnotesize}
            \nodepart{two}
                \texttt{\_\_init\_\_.py}
            \nodepart{three}
                \texttt{model.py}
            \nodepart{four}
                \texttt{predict.py}
        };

        \node (6) [
            rectangle split,
            rectangle split parts=3,
            rectangle split part fill={
                gray,
                blue!50,
                blue!50,
            },
            draw,
            text width=2.5cm,
            below=of 5
        ]
        {
            \nodepart{one}
                \begin{footnotesize}
                    final
                \end{footnotesize}
            \nodepart{two}
                \texttt{\_\_init\_\_.py}
            \nodepart{three}
                \texttt{plot.py}
        };

        \draw[-, out=20, in=165] (3) to (4);
        \draw[-, out=-2, in=165] (3) to (5);
        \draw[-, out=-14, in=165] (3) to (6);
    \end{tikzpicture}}
\end{tiny}

\end{document}
