def build(ctx):

    source = 'albouy_2012'

    ctx(
        features='tex',
        source=source + '.tex',
        prompt=1,
        name=source
    )
    ctx.install_files(
        ctx.env.PROJECT_PATHS['PROJECT_ROOT'].abspath(),
        source + '.pdf'
    )
